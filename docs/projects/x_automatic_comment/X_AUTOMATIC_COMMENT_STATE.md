# X Automatic Comment 状态（STATE）

- 更新时间：2026-03-28
- 归档类型：代码归档（非纯讨论）
- 主路径：`D:\code\Chrome\X Automatic Comment`

## 本线程产出（OUTPUT）
1. 自动运行控制改为单一切换按钮：运行中显示“停止自动回复”，空闲时显示“开始自动回复”。
2. 在 `startAuto()` 启动和 `stopAuto()` 停止时补充 `render()`，确保按钮文案和面板状态实时刷新。
3. 搜索拼装规则修正为用户可理解格式：`(词1 OR 词2) min_replies:1 -filter:replies`，不再自动追加 `(gm OR gn)`。
4. 搜索配置 UI 收敛：
   - 包含词改为“双输入框 + 或（OR）”。
   - 移除 `启用 GM / 启用 GN` 开关。
   - 文案统一为“最小评论数”。
5. 弹窗账户区与入口区继续收敛：
   - 去掉“暗黑 + 绿色增强层”副标题与冗余登录状态行。
   - 去掉 Supabase host 展示。
   - 去掉“打开 X 搜索”按钮（保留高级面板/详细配置入口）。
   - 登录区保留邮箱 + 小号退出按钮（未登录时显示登录按钮）。
6. UI 文案层统一：界面中 `Spark/星火` 展示统一替换为 `AI`，底部改为版本号显示。

## 关键状态（STATE）
- 已改动源码文件：
  - `D:\code\Chrome\X Automatic Comment\xac-content.js`
  - `D:\code\Chrome\X Automatic Comment\xac-ui.js`
- 当前行为：
  - 自动任务启动后按钮立即切换为停止态；停止后立即恢复启动态。
  - 搜索词提交遵循 X 语法：仅由“包含词/排除词 + 最小评论数 + 排除回复流”组成。
  - 弹窗登录区域信息更简洁，不再展示与用户操作无关的环境细节。
- 已知未完成：
  - 尚未执行完整人工回归（搜索链路 + 自动执行链路 + 登录态切换全流程）。

## Next Step
1. 在 X 页面做端到端验证：
   - 搜索拼装：输入双词后确认 URL 语法正确；
   - 自动执行：开始 -> 停止 -> 再开始，确认单按钮状态稳定；
   - 登录区：未登录/已登录切换时按钮与邮箱展示一致。
2. 若继续做 UI 收敛，统一弹窗与侧边栏的术语（“评论/回复”）和字段顺序。
