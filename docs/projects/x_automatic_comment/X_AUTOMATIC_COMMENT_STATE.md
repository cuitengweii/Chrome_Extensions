# X Automatic Comment 状态（STATE）

- 更新时间：2026-03-28
- 归档类型：代码归档（非纯讨论）
- 主路径：`D:\code\Chrome\X Automatic Comment`

## 本线程产出（OUTPUT）
1. 自动运行控制改为单一切换按钮：运行中显示“停止自动回复”，空闲时显示“开始自动回复”。
2. 在 `startAuto()` 启动和 `stopAuto()` 停止时补充 `render()`，确保按钮文案和面板状态实时刷新。
3. 搜索拼装规则修正为用户可理解格式：`(词1 OR 词2) min_replies:1 -filter:replies`，不再自动追加 `(gm OR gn)`。
4. 搜索配置 UI 收敛：
   - 包含词改为“双输入框 + 或（OR）”。
   - 移除 `启用 GM / 启用 GN` 开关。
   - 文案统一为“最小评论数”。
5. 弹窗账户区与入口区继续收敛：
   - 去掉“暗黑 + 绿色增强层”副标题与冗余登录状态行。
   - 去掉 Supabase host 展示。
   - 去掉“打开 X 搜索”按钮（保留高级面板/详细配置入口）。
   - 登录区保留邮箱 + 小号退出按钮（未登录时显示登录按钮）。
6. UI 文案层统一：界面中 `Spark/星火` 展示统一替换为 `AI`，底部改为版本号显示。
7. 新增可复跑回归脚本 `tools/xac-e2e-regression.js`，并在真实 X 页面完成自动运行链路回归：
   - 校验单一运行切换按钮存在且旧双按钮不存在；
   - 校验“开始自动回复 -> 停止自动回复 -> 开始自动回复 -> 停止”状态文案一致；
   - 校验运行计数在指示器中可见且停止后指示器关闭。

## 关键状态（STATE）
- 已改动源码文件：
  - `D:\code\Chrome\X Automatic Comment\xac-content.js`
  - `D:\code\Chrome\X Automatic Comment\xac-ui.js`
  - `D:\code\Chrome\X Automatic Comment\tools\xac-e2e-regression.js`
- 当前行为：
  - 自动任务启动后按钮立即切换为停止态；停止后立即恢复启动态。
  - 搜索词提交遵循 X 语法：仅由“包含词/排除词 + 最小评论数 + 排除回复流”组成。
  - 弹窗登录区域信息更简洁，不再展示与用户操作无关的环境细节。
  - 已回归验证：在 `https://x.com/` 上单按钮切换、状态文案、计数展示与停止行为一致。
- 已知未完成：
  - 搜索拼装 URL 与登录态切换链路仍需单独补做一轮端到端验证。

## Next Step
1. 在 X 页面补回归搜索链路：输入双词 + 排除词 + 最小评论数，确认最终 URL 与预览一致。
2. 在登录态切换场景复测弹窗账号区：未登录/已登录的按钮与邮箱展示是否一致。
3. 若继续做 UI 收敛，统一弹窗与侧边栏的术语（“评论/回复”）和字段顺序。
